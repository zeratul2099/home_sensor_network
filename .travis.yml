language: python
python:
    - "3.5"
    - "3.6"
    - "3.7"

install:
    - "pip install -r requirements.txt"
    - "pip install pylint"

script:
    - "python -m py_compile *.py"
    - "pylint *.py -f colorized"
