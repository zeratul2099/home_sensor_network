language: python
python:
    - "3.5"
    - "3.6"
    - "3.7-dev"

cache: pip

install:
    - "pip install poetry"
    - "poetry install --nodev"
    - "pip install pylint"

script:
    - "python -m py_compile *.py"
    - "pylint *.py -f colorized"
